# Rename this file to just 'secret.sls'; it'll be gitignored.
# Then you can populate it
concourse_local_user:
concourse_local_password:
concourse_main_team_local_user:
concourse_add_local_user:

concourse_postgres_host:
concourse_postgres_user:
concourse_postgres_password:
concourse_postgres_database:

concourse_tsa_host:
concourse_external_url:

# concourse_tls_bind_port: 443
# concourse_enable_lets_encrypt: true
