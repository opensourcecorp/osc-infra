# You will need to change these yourself, since we can't make assumptions and
# dummy them out of the gate
keypair_name       = "your-ec2-keypair-name"
keypair_local_file = "your-ec2-keypair-PEM-file"
